# ============================================================
#  Environment: PRODUCTION  (committed config — NO secrets)
#  Loaded via: npm run dev:prod / build:prod / start:prod
# ============================================================

# Upstream SCA API proxied by next.config.ts rewrite.
# NO trailing slash, NO /api suffix.
API_UPSTREAM=https://sca-api.concung.com
