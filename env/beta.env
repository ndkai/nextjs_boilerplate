# ============================================================
#  Environment: BETA  (committed config — NO secrets)
#  Loaded via: npm run dev:beta / build:beta / start:beta
# ============================================================

# Upstream SCA API proxied by next.config.ts rewrite.
# NO trailing slash, NO /api suffix.
# NOTE: beta hiện trỏ cùng upstream với UAT (theo data cung cấp).
API_UPSTREAM=https://sca-api-uat.concung.vn
